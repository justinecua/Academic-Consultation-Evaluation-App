import { View } from 'react-native';
import { consultationStyles as styles } from '../../styles/consultationStyles';

const ConsultationSeparator = () => <View style={styles.separator} />;

export default ConsultationSeparator;
